---
# Hiera module layer configuration (https://docs.puppet.com/puppet/4.10/hiera_layers.html#the-module-layer)
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Module default data"
    paths:
        - "common.yaml"
